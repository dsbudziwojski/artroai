import {NavLink} from "react-router-dom";

function Login() {
    return (
        <>
            <NavLink to="/home">
                <button>ENTER</button>
            </NavLink>
        </>
    );
}

export default Login;